# 斗鱼开播提醒 · 看门狗配置
#
# 安装位置：/etc/default/douyu-watchdog（systemd 单元用 EnvironmentFile 读它）
# 安装方式：install-watch.sh 会自动装一份；若已存在则**绝不覆盖**。
#   cp watchdog.env.example /etc/default/douyu-watchdog
#   chmod 600 /etc/default/douyu-watchdog      # 里面有 webhook 密钥，别让别的用户读到
#   systemctl restart douyu-watchdog.timer     # 改完要重启定时器才生效
#
# 语法：KEY=value，**不要**写 export、不要加引号、不要留行尾空格。
# 下面所有项都可以不填 —— 不填就是括号里的默认值。

# ─────────────────────────────────────────────────────────────────────────
# 一、告警发到哪（这是唯一需要你花 5 分钟决定的事）
# ─────────────────────────────────────────────────────────────────────────
#
# 为什么重要：NapCat 掉线时，它自己发不出消息 —— 所以告警必须走一条
#             **不经过 NapCat** 的通道，否则「掉线告警」等于没说。
#
# 三个可选通道（配了几个就同时用几个）：
#
#   1. ALERT_WEBHOOK —— 通用 webhook。**唯一在 NapCat 挂掉时还能到达你的
#      通道，强烈建议至少配这一个。** 微信接收见下方「想推微信」。
#   2. ALERT_ONEBOT_PRIVATE —— 用 NapCat 私聊你自己。零成本、即时，
#      但它依赖 NapCat 在线，只能覆盖「watch.py 停摆」这类故障。
#   3. ALERT_EMAIL —— 走本机 msmtp/sendmail，需要服务器上有可用的发信配置。
#
# ── 想推到微信 ──────────────────────────────────────────────────────────
#
#   两家免费、都能推到微信，差别很大（2026-09 核实的官方口径）：
#
#   |                | pushplus 推送加        | Server酱 Turbo        |
#   |----------------|------------------------|-----------------------|
#   | 免费额度       | 实名后 200 次/天       | 5 条/天               |
#   | 微信里能看到   | 标题 + 完整正文        | **只有标题**，正文看不到 |
#   | 要不要实名     | 要（手机号）           | 不要                  |
#   | 网址           | pushplus.plus          | sct.ftqq.com          |
#
#   免费额度 5 条/天 对告警是**不够的**：一次掉线持续 4 小时就会发掉 4 条
#   （同一问题每小时最多提醒一次），再加每天一条「一切正常」就满了。
#   所以推微信优先用 pushplus；两个都配最稳（下面有示例）。
#
#   拿 pushplus 的 token：pushplus.plus 微信扫码登录 → 实名认证 →
#   「一对一推送」页复制 token。
#
#   拿 Server酱的 SendKey：sct.ftqq.com 微信扫码 → SendKey 页复制，形如 SCTxxxx。
#   ⚠️ Server酱有两个产品，SendKey 不通用、地址也不同：
#         Turbo（SCT 开头）→ https://sctapi.ftqq.com/<SendKey>.send
#         自定义域名（sctp 开头）→ https://<uid>.push.ft07.com/send/<SendKey>.send
#      （uid 是 key 里 sctp 与 t 之间的那串数字）。key 和域名对不上会发不出去，
#      看门狗在启动检查里会直接提醒你换过来。
#
#   其它能推到微信的：企业微信群机器人（ALERT_WEBHOOK_KIND=wecom，不需要认证，
#   建个只有自己的群也行，但要装企业微信 App 才能收到）。
#
# ── 其它通道 ────────────────────────────────────────────────────────────
#
#   ・Bark（iOS，装 App 就有 URL）  ：kind=bark
#   ・Telegram Bot                  ：kind=telegram（另需 ALERT_TG_CHAT_ID）
#   ・飞书/钉钉群机器人             ：kind=feishu / dingtalk
#   ・自建服务 / n8n / IFTTT        ：kind=generic，发
#     {"title":..,"text":..,"level":"alert","host":..,"time":..}
#
# 无论配不配，告警都会**无条件**写一行到 /var/lib/douyu-watchdog/alerts.log，
# 所以不配也不会丢，只是没人叫醒你 —— 那句「没有配置任何独立于 NapCat 的
# 告警通道」的提示是故意的，不假装成功。

# 你的主 QQ 号（不是机器人小号）。填了就用私聊发告警到你自己。
# 刻意不给机器人私聊发到**群**：掉线告警发群里会打扰一堆人，没意义。
ALERT_ONEBOT_PRIVATE=

# 告警地址。可以写一条，也可以用 `;` 分开写多条（互相之间不影响，
# 一条挂了另一条照发）。写法：`类型|地址`，不写类型就用下面的 ALERT_WEBHOOK_KIND。
#
# 只推微信（推荐，pushplus 200 条/天）：
#   ALERT_WEBHOOK=pushplus|https://www.pushplus.plus/send?token=你的token
#
# 微信双通道（最稳，pushplus 主力 + Server酱 兜底）：
#   ALERT_WEBHOOK=pushplus|https://www.pushplus.plus/send?token=你的token;serverchan|https://sctapi.ftqq.com/SCTxxxxxx.send
#
# token 放在地址的查询串里就行，看门狗会取出来塞进请求体，
# 而且打印日志/写告警正文时会把查询串隐去，不会泄露。
ALERT_WEBHOOK=

# 地址里没写 `类型|` 时用这个类型。
# generic / serverchan / pushplus / bark / telegram / dingtalk / feishu / wecom
ALERT_WEBHOOK_KIND=generic

# 仅 telegram 需要：你的 chat_id（可以先给 bot 发一条消息，再访问
#   https://api.telegram.org/bot<token>/getUpdates 里拿）
ALERT_TG_CHAT_ID=

# 邮件通道（可选）。需要本机已装好 msmtp 或 sendmail，本脚本不替你装。
ALERT_EMAIL=
# 发件人地址，不填则用 douyu-watchdog@<主机名>
# ALERT_EMAIL_FROM=

# ─────────────────────────────────────────────────────────────────────────
# 二、判定阈值（不知道就别动）
# ─────────────────────────────────────────────────────────────────────────

# 心跳陈旧多久算「watch.py 没在跑」，单位秒。默认 420（7 分钟）。
# watch.py 每分钟一跳，连着 7 跳没来才判定，不会误报。
# ⚠️ 心跳日志里只有时间没有日期，所以新鲜度看的是日志文件修改时间，不是解析出来那个时刻。
STALE_SECONDS=420

# 心跳序号连续几轮不前进算「卡死」。默认 3（看门狗每 2 分钟一轮 ≈ 6 分钟）。
# 和「没在跑」不是一回事：卡死 = 日志在写、但每轮都在抛错（例如斗鱼接口挂了）。
STUCK_ROUNDS=3

# 连续几轮发现 NapCat 不通才动手。默认 3（≈6 分钟），避免把它自己的慢启动当成故障。
FAIL_THRESHOLD=3

# ─────────────────────────────────────────────────────────────────────────
# 三、自愈策略
# ─────────────────────────────────────────────────────────────────────────

# 1=允许自动 docker restart napcat，0=只告警不动手。
# 敢开自动重启的前提是「重启能免扫码自动登录」—— 那一步必须已经验收通过
# （见 DEPLOY.md「重启免扫码验收」）。没验过就先设 0。
AUTO_RESTART=1

# 两次自动重启之间至少间隔多少秒，默认 900（15 分钟）。防止重启风暴。
RESTART_COOLDOWN=900

# 一天最多自动重启几次，默认 6。到顶就停手转人工告警。
# 为什么要有这个上限：反复重启通常是内存不足或配置错误，
# 继续重启只会让 NapCat 和你的个人网页一起雪崩。
MAX_RESTARTS_PER_DAY=6

# ─────────────────────────────────────────────────────────────────────────
# 四、告警频率
# ─────────────────────────────────────────────────────────────────────────

# 同一个问题最多多久提醒一次（秒），默认 3600。同一故障不会每 2 分钟刷你一次。
ALERT_REPEAT_SECONDS=3600

# 每天这个时刻之后，如果一切正常，就发一条「一切正常」。
# 这是**沉默即出事**的兜底：哪天你没收到这条，说明机器或看门狗自己挂了。
# 留空 = 不发。建议填你每天一定会看手机的时间，例如 20:00。
DAILY_OK_AT=

# 这条发失败时怎么办。默认不写就是：含首次最多试 5 次，首次失败后隔 3 分钟、
# 然后 6、12、24 分钟（封顶 30）再来，试满还失败才放弃并写进 alerts.log。
# 注意：**只有真送到一条通道才记「今天已报」**，所以一次网络抽风不会把它吞掉。
# DAILY_OK_RETRY_MAX=5
# DAILY_OK_RETRY_BACKOFF_MINUTES=3

# ---- 「报平安」那条长什么样（都可选，留空用内置文案）----
# 改这里就行，不用动 watchdog.py —— 本文件 install-watch.sh 从不覆盖，
# 所以升级看门狗也不会把这几行冲掉。
# DAILY_OK_TITLE=报个平安
# DAILY_OK_BODY=整条链路都在。这条每天发一次，哪天没收到就是出事了。
# 颜文字：可以写多个（逗号或空格分隔），按日期轮换，同一天固定同一个。
# DAILY_OK_KAOMOJI=(๑•̀ㅂ•́)و✧,✧*｡٩(ˊᗜˋ*)و✧*｡

# ─────────────────────────────────────────────────────────────────────────
# 五、路径与对象（一般不用改）
# ─────────────────────────────────────────────────────────────────────────

# APP_DIR=/opt/douyu-live-notify
# TICK_LOG=/var/log/douyu-watch/tick.log
# STATE_DIR=/var/lib/douyu-watchdog
# NAPCAT_CONTAINER=napcat
# CONFIG=/opt/douyu-live-notify/config.json
# HTTP_TIMEOUT=10
# CMD_TIMEOUT=60

# OneBot 地址与 token 默认从 config.json 里读，不用在这里重复一遍。
# 万一你想指向另一个实例再往下填：
# ONEBOT_BASE=http://127.0.0.1:3000
# ONEBOT_TOKEN=
