{
  "_comment": "复制成 config.json 再改。room_id 要填真实房间号（不是靓号），填法见 watch.py 文件头。",

  "room_id": "YOUR_ROOM_ID",
  "room_url": "",

  "interval_seconds": 45,
  "jitter_seconds": 8,
  "confirm_rounds": 2,

  "notify_on_end": false,
  "treat_loop_as_live": false,
  "log_heartbeat": true,

  "_comment_retry": "发送失败会自动补发（默认开）：正文记进 state_*.json 的 notify 字段，按 1/2/4/8… 分钟退避重试。可调 retry_failed_notify / notify_retry_max / notify_retry_max_age_hours / notify_retry_backoff_cap_minutes。注意 console 通道不算「送达」。",

  "channels": ["onebot"],

  "onebot": {
    "base": "http://127.0.0.1:3000",
    "token": "YOUR_ONEBOT_TOKEN",
    "target_type": "group",
    "target_id": "YOUR_GROUP_ID",
    "at_all": false,
    "at_users": []
  },

  "qq_official": {
    "app_id": "",
    "client_secret": "",
    "target_type": "c2c",
    "group_openid": "",
    "user_openid": ""
  }
}
